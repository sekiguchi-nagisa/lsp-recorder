#!/usr/bin/env arsh

source $SCRIPT_DIR/record.arsh

var bin = 'arshd'
if (test -x $SCRIPT_DIR/../arsh/build-asan/tools/analyzer/arshd) {
    $bin = "$SCRIPT_DIR/../arsh/build-asan/tools/analyzer/arshd".realpath()
}

record --bin $bin -- $@